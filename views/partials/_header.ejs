<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
      </div>
    </div>
      <div>
       <% if (user) { %>
        <form method="POST" action="/logout" class="form-inline" style="margin-right: 4px">
          <h6>Logged in as: <%=user.email%></h6>
          <button type ="submit" class="btn btn-secondary active" style="margin-left: 10px"> Logout</button>  
      </form>
        <% } else { %> 
        <div class="input-group">
          <a class="btn btn-secondary active" href="/register" style="margin-right: 10px">Register</a>
          <a class="btn btn-secondary active" href="/login">Login</a>

      </div>
        <% } %>
    </div>
  </nav>
</header>